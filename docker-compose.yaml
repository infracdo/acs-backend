services:

  frontend:
    build:
      context: /home/$USER/acs-frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"  # Change port as necessary
      - "443:443"
    networks:
      - app-network
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/app/src
      - ./target:/app/target
      - ./logs:/app/logs
    ports:
      - "7547:7547"
    networks:
      - app-network
    depends_on:
      - frontend
    command: ./mvnw spring-boot:run
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
